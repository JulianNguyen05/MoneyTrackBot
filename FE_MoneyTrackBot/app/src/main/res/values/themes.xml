<resources xmlns:tools="http://schemas.android.com/tools">

    <!-- Base application theme -->
    <style name="Base.Theme.FE_MoneyTrackBot" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Bạn có thể custom màu ở đây nếu muốn -->
        <!-- <item name="colorPrimary">@color/...</item> -->
    </style>

    <!-- Theme chính của app -->
    <style name="Theme.FE_MoneyTrackBot" parent="Base.Theme.FE_MoneyTrackBot" />

    <!-- Bottom nav text (inactive) -->
    <style name="BottomNav.Text.Inactive" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/poppins_regular</item>
    </style>

    <!-- Bottom nav text (active) -->
    <style name="BottomNav.Text.Active" parent="TextAppearance.AppCompat.Caption">
        <item name="android:textSize">12sp</item>
        <item name="fontFamily">@font/poppins_medium</item>
    </style>

    <style name="CustomDatePickerTheme" parent="ThemeOverlay.MaterialComponents.MaterialCalendar">
        <item name="colorSurface">#F5F5F5</item>

        <item name="colorOnSurface">@color/black</item>

        <item name="colorPrimary">@color/normal_weight</item>

        <item name="colorOnPrimary">@color/white</item>

        <item name="fontFamily">@font/poppins_medium</item>
    </style>

</resources>
